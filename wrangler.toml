name = "reframe-houses"
compatibility_date = "2024-11-01"
pages_build_output_dir = "."

# This is a static site + Pages Functions (functions/api/lead.js).
# Secrets are NOT stored here. Set the monday.com token with:
#   wrangler pages secret put MONDAY_TOKEN
# ...or in the Cloudflare dashboard: Workers & Pages → reframe-houses →
#   Settings → Variables and Secrets.
