name = "reframe-houses"
compatibility_date = "2024-11-01"
pages_build_output_dir = "public"

# Cloudflare Pages project. Static site lives in ./public and the lead-capture
# API is the Pages Function at functions/api/lead.js.
#
# Secret (set in the Pages project dashboard, NOT here):
#   Settings → Variables and secrets → add MONDAY_TOKEN as an encrypted secret.
